name: release
on:
  push:
    branches:
    - main
  workflow_dispatch:

jobs:
  build:
    runs-on: build
    steps:
      - uses: actions/checkout@v4
      - name: Build application
        run: |
          echo "Building"
